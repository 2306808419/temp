#include <stdarg.h>
#include "printf.h"

void printf(char *fmt,...) {
    /**
     * 观察参数列表中的...，尝试搜索c中的可变参数来完成这个函数
     * 这里使用了头文件stdarg.h，你可以在你的linux终端中输入`man stdarg`来得到有关可变参数的信息
     * 你可以不必一次性实现你所需的所有功能，但你总会在未来的某个时刻需要实现他们
     */
    return;
}